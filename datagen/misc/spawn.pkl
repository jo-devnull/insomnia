const default {
  result = "default"
  hostile = true
  dimension = "overworld"
}

const allow = (default) {
  result = "allow"
}

const deny = (default) {
  result = "deny"
}

const customMobWeight = new Listing {
  "minecraft:drowned"
  "cataclysm:deepling"
  "cataclysm:deepling_brute"
  "cataclysm:deepling_angler"
  "cataclysm:deepling_priest"
  "cataclysm:deepling_warlock"
  "upgrade_aquatic:thrasher"
}

rules {
  new {
    result = "deny"
    dimension = "superflat:superflat"
  }

  (default) {
    mod {
      "alexsmobs"
      "alexscaves"
      "aquamirae"
      "mowziesmobs"
      "cataclysm_spellbooks"
      "enderman_overhaul"
    }
  }

  (deny) {
    mod {
      "zoniex"
    }

    biome {
      "alexscaves:candy_cavity"
      "alexscaves:abyssal_chasm"
      "alexscaves:forlorn_hollows"
      "alexscaves:magnetic_caves"
      "alexscaves:primordial_caves"
      "alexscaves:toxic_caves"
    }
  }

  (default) {
    spawner = true
  }

  (default) {
    lunar {
      "enhancedcelestials:blood_moon"
      "enhancedcelestials:super_blood_moon"
    }
  }

  (deny) {
    mob = customMobWeight
    random = 0.95
  }
  
  (deny) {
    lunar {
      "enhancedcelestials:blue_moon"
    }
    
    minheight = 27
  }
  
  (default) {
    structuretags { "modpack:always_unsafe" }
  }

  (deny) {
    mod = "fromtheshadows"
    biometags = "forge:is_cave"
    minheight = 0
  }

  (deny) {
    biometags = "modpack:kinda_safe"
    minheight = 40
  }

  (default) {
    biometags = "modpack:always_unsafe"
  }

  (deny) {
    minheight = 54
  }
}

phases = new Listing {}
