const default {
  result = "default"
  hostile = true
  dimension = "overworld"
}

const allow = (default) {
  result = "allow"
}

const deny = (default) {
  result = "deny"
}

const surfaceLevel = 54

const reducedSpawns {
  "minecraft:creeper"
  "minecraft:drowned"
  "minecraft:skeleton"
}

rules {
  new {
    result = "deny"
    dimension = "superflat:superflat"
  }

  (default) {
    mod {
      "alexsmobs"
      "alexscaves"
      "benssharks"
      "aquamirae"
    }
  }

  (default) {
    spawner = true
  }

  (default) {
    mob {
      "fromtheshadows:nehemoth"
      "minecraft:vindicator"
      "minecraft:pillager"
    }
  }

  (deny) {
    biometags = "modpack:safe"
    minheight = 40
  }

  (default) {
    lunar { "blood_moon" "eclipse_moon" "crimson_moon" }
  }

  (deny) {
    mob { "minecraft:drowned" }
    random = 0.75
  }

  (deny) {
    mob = reducedSpawns
    random = 0.6
  }

  (deny) {
    random = 0.5
    continue = true
  }

  (default) {
    biometags = "modpack:unsafe"
  }

  (deny) {
    minheight = 54
  }
}

phases = new Listing {}
